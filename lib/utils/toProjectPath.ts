// export function toProjectPath(fullPath: string): string {
//   const cwd = process.cwd();
//   console.log("cwd", cwd);
//   return fullPath.startsWith(cwd) ? fullPath.slice(cwd.length + 1) : fullPath;
// }
